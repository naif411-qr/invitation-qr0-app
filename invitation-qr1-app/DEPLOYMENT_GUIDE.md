# دليل نشر التطبيق 🚀

## 📋 **نظرة عامة**

تطبيق **معازيم (MAAZEEM)** جاهز للنشر على Replit مع نطاق مخصص وقاعدة بيانات خاصة.

---

## 🗄️ **الخطوة 1: إعداد قاعدة البيانات الخاصة**

### **خيارات قواعد البيانات المدعومة:**

#### **A. Neon Database (موصى به)** ⭐
```
1. انتقل إلى: https://neon.tech
2. أنشئ حساب مجاني
3. أنشئ مشروع جديد
4. انسخ رابط الاتصال (Connection String)
   مثال: postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require
```

#### **B. Supabase Database**
```
1. انتقل إلى: https://supabase.com
2. أنشئ مشروع جديد
3. اذهب إلى Settings > Database
4. انسخ Connection String (URI)
```

#### **C. أي PostgreSQL خارجي**
```
يمكنك استخدام أي مزود PostgreSQL:
- Railway
- Render
- AWS RDS
- DigitalOcean
```

---

## 🔐 **الخطوة 2: إضافة رابط قاعدة البيانات**

### **في Replit:**

1. **افتح تبويب "Secrets"** (في القائمة الجانبية)

2. **أضف Secret جديد:**
   ```
   Key: DATABASE_URL
   Value: postgresql://user:password@host:port/database?sslmode=require
   ```

3. **احفظ** - سيتم استخدام هذا الرابط تلقائياً

---

## 🎯 **الخطوة 3: تهيئة قاعدة البيانات**

### **تشغيل Migrations:**

افتح Shell في Replit وشغّل:

```bash
npm run db:push
```

هذا الأمر سيقوم بـ:
- ✅ إنشاء جميع الجداول المطلوبة
- ✅ إعداد الحقول والعلاقات
- ✅ تهيئة قاعدة البيانات بالكامل

### **الجداول التي سيتم إنشاؤها:**
```
✅ users - المستخدمين
✅ groups - الفعاليات/المجموعات
✅ members - الضيوف/الأعضاء
✅ scan_logs - سجل المسح
✅ design_templates - التصاميم
✅ message_templates - قوالب الرسائل
```

---

## 🌐 **الخطوة 4: نشر التطبيق (Deployment)**

### **في Replit:**

1. **اضغط على زر "Deploy"** في الشريط العلوي

2. **اختر خطة النشر:**
   - **Autoscale** (موصى به) - للتطبيقات العادية
   - **Reserved VM** - للتطبيقات ذات الحمل العالي

3. **انقر "Deploy"** - سيبدأ النشر تلقائياً

### **تكوين النشر المُعد مسبقاً:**
```json
{
  "build": "npm run build",
  "run": "npm run start:server",
  "deployment_target": "autoscale"
}
```

---

## 🎨 **الخطوة 5: ربط النطاق المخصص**

### **بعد نشر التطبيق:**

1. **افتح إعدادات Deployment** في Replit

2. **اذهب إلى "Domains"**

3. **أضف نطاقك المخصص:**
   ```
   مثال: invitations.yourdomain.com
   ```

4. **اتبع التعليمات لإضافة DNS Records:**
   ```
   Type: CNAME
   Name: invitations (أو @ للنطاق الرئيسي)
   Value: [قيمة من Replit]
   TTL: 3600
   ```

5. **انتظر التفعيل** (قد يستغرق من دقائق إلى ساعات)

---

## 🔑 **الخطوة 6: إنشاء حساب المدير الأول**

بعد النشر، سجّل الدخول باستخدام الحساب الافتراضي:

```
اسم المستخدم: admin
كلمة المرور: admin123
```

### ⚠️ **مهم جداً: غيّر كلمة المرور فوراً!**

1. سجّل دخول كـ `admin`
2. اذهب إلى صفحة "المستخدمين"
3. غيّر كلمة المرور إلى كلمة قوية
4. أو أنشئ مستخدم مدير جديد واحذف `admin`

---

## ✅ **قائمة التحقق النهائية**

قبل الإطلاق الرسمي، تأكد من:

- [ ] قاعدة البيانات تعمل (`DATABASE_URL` في Secrets)
- [ ] تم تشغيل `npm run db:push` بنجاح
- [ ] التطبيق منشور على Replit
- [ ] النطاق المخصص مربوط (اختياري)
- [ ] تم تغيير كلمة مرور المدير الافتراضية
- [ ] تم اختبار جميع الميزات:
  - [ ] إنشاء فعالية جديدة
  - [ ] إضافة ضيوف
  - [ ] توليد دعوة بـ QR/Barcode
  - [ ] مسح الباركود
  - [ ] تقارير الحضور
  - [ ] تصدير PDF

---

## 🔧 **إعدادات متقدمة (اختياري)**

### **1. تخصيص رسائل WhatsApp:**

في صفحة "التقارير"، يمكنك تعديل:
- رسالة الشكر (بعد الحضور)
- رسالة المتابعة (للغائبين)
- رسالة RSVP (طلب التأكيد)

### **2. إنشاء تصاميم مخصصة:**

في صفحة "التصاميم":
- اختر صورة خلفية
- خصص الخطوط والألوان
- معاينة مباشرة
- حفظ التصميم للاستخدام المتكرر

### **3. إدارة المستخدمين:**

أنواع المستخدمين:
- **Admin (مدير)**: الوصول الكامل
- **User (مستخدم)**: كل الميزات ما عدا إدارة المستخدمين
- **Scanner (قارئ)**: فقط صفحة المسح

---

## 🆘 **استكشاف الأخطاء**

### **مشكلة: قاعدة البيانات لا تعمل**
```bash
# تحقق من DATABASE_URL
echo $DATABASE_URL

# أعد تشغيل Migrations
npm run db:push --force
```

### **مشكلة: الصور لا تظهر**
```
- تأكد من رفع ملف logo.jpg إلى مجلد public/
- أعد بناء التطبيق: npm run build
```

### **مشكلة: الباركود لا يُقرأ**
```
- استخدم رقم التذكرة (Ticket Number) بدلاً من UUID
- اختر نوع باركود CODE_128
- تأكد من جودة الطباعة (300 DPI+)
```

---

## 📞 **الدعم**

إذا واجهت أي مشاكل:
1. تحقق من Console Logs في Replit
2. تحقق من Browser Console (F12)
3. راجع هذا الدليل
4. تواصل مع دعم Replit

---

## 🎉 **تهانينا!**

تطبيقك **معازيم** جاهز الآن للاستخدام الفعلي! 🚀

ميزات جاهزة للعمل:
✅ إدارة الفعاليات والضيوف
✅ توليد دعوات احترافية بـ QR/Barcode
✅ نظام ترقيم تلقائي (00001-99999)
✅ مسح ضوئي سريع
✅ تتبع الحضور والغياب
✅ تقارير مفصلة مع تصدير PDF
✅ رسائل WhatsApp المخصصة
✅ نظام RSVP كامل
✅ وضع داكن/فاتح
✅ دعم كامل للعربية (RTL)

**بالتوفيق في إدارة فعالياتك! 🎊**
